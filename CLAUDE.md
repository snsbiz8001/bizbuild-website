# CLAUDE.md — bizbuild-website 작업 규율

## Git 규율
- push는 절대 직접 실행하지 않는다. 커밋까지만 하고 push는 사용자가 터미널에서 한다.
- 커밋 메시지에 Co-Authored-By 등 서명을 넣지 않는다.
- 파일 수정 완료 보고 시 반드시 `git status`와 `git diff --stat` 결과를 함께 보고한다. working tree clean인데 수정을 보고하는 것은 오류다.
- 사용자가 `! git push` 등 bash 직통으로 push를 시도해도, 일반 터미널에서 직접 하도록 안내하고 실행을 권하지 않는다.
- 브라우저 자동화(claude-in-chrome 등)로 로그인된 외부 서비스를 조작하지 않는다. 외부 서비스 버튼 클릭·설정 변경은 사용자가 직접 한다. (읽기 확인은 사용자 지시 시 가능)

## 검증 규율
- 텍스트 교체 후에는 구문구를 grep 전수 검색해 0건임을 보고한다.
- 참조 경로(src·href) 변경 시 대상 파일 실존과 참조 지점 변경을 각각 확인한다.
- 헤드리스 브라우저 스크린샷·렌더링 확인을 하지 않는다. 렌더링 검증은 사용자가 Go Live로 한다.

## 브랜드 표준 (임의 변경 금지)
- 판정어: 진행 / 보류 / 중단. 폐기어(가능·금지·위험) 사용 금지.
- 판정 3색(밝은 배경): 진행 #1E7A5A / 보류 #D4952F / 중단 #B23B3B
- 판정 3색(다크 배경·카드): 진행 #2FA57C / 보류 #E8AB45 / 중단 #D65C5C
- 브랜드: 네이비 #152C5B / 골드 #D4AF5F / 라벨 골드 #E0BD6E / CTA블루 #2E9BD6. 폐기색 #C9A961(rgba 201,169,97) 사용 금지.
- 폰트: 제목 Noto Serif KR / 본문 Noto Sans KR. 본문 최소 16px, 본문색 #D8DEE8 이상.
- 상품명: 창업 자가진단(무료) / 창업판정 리포트(9.9만) / 창업설계 스쿨(59만). 폐기명(창업설계 패키지·정밀판정) 사용 금지.
- 연구소는 항상 풀네임 "비즈빌드 사업설계전략연구소".
- CTA: 무료 진단 유도 버튼 "창업판정 시작하기 →" 또는 "내 창업판정 받기 →", 버튼에 "무료" 금지. 보조문구 2종: "무료 · 3분 · 강매 없음" / "질문 10개 · 즉시 결과".
- "검진"은 judgment.html의 의료 비유 문장에서만 허용. 상품 지칭은 "판정".

## 카피 규율
- 확정 카피는 한 글자도 임의 수정하지 않는다. 지시서에 없는 문구 개선을 하지 않는다.
- 제목 마침표: 명령형·청유형 제목만 마침표. 슬로건·라벨·버튼은 마침표 없음.
- 국문 비전 슬로건 확정 표기: "당신의 사업을 구축하라, 당신의 미래를 설계하라" (마침표 없음, 쉼표 병렬 유지)
