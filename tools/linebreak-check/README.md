# 줄바꿈 측정

1. 실행은 독립 WSL 터미널에서 한다 (CLI 세션 안에서 돌리지 않는다). 처음 한 번은 `cd tools/linebreak-check && npm run setup`, 이후 `node check.js <URL>`.
2. 이건 1차 확인용 측정값일 뿐 최종이 아니다. 요소별 줄 수와 줄별 텍스트만 나오고, 좋다·나쁘다 판단은 하지 않는다.
3. 최종 확인은 반드시 실기기(실제 폰)로 사람이 한다.
